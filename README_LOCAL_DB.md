# Локальная база данных в Docker

## ✅ Статус
- **MySQL контейнер**: Запущен и работает
- **База данных**: `lk_tk82_ru` создана
- **Миграции**: Все выполнены успешно
- **Приложение**: http://localhost:8000

## 📊 Информация о БД

- **Хост**: `mysql` (внутри Docker сети) или `127.0.0.1` (снаружи)
- **Порт**: `3306`
- **База данных**: `lk_tk82_ru`
- **Пользователь**: `laravel`
- **Пароль**: `laravel_password`
- **Root пароль**: `root_password`

## 🔧 Команды для работы с БД

### Подключиться к MySQL из контейнера:
```bash
docker-compose exec mysql mysql -ularavel -plaravel_password lk_tk82_ru
```

### Подключиться к MySQL снаружи Docker:
```bash
mysql -h 127.0.0.1 -P 3306 -ularavel -plaravel_password lk_tk82_ru
```

### Выполнить миграции:
```bash
docker-compose exec app php artisan migrate
```

### Откатить миграции:
```bash
docker-compose exec app php artisan migrate:rollback
```

### Статус миграций:
```bash
docker-compose exec app php artisan migrate:status
```

### Запустить сидеры (если есть):
```bash
docker-compose exec app php artisan db:seed
```

## 💾 Персистентность данных

Данные БД сохраняются в Docker volume `calcs_mysql_data`. Даже после остановки контейнеров данные останутся.

### Удалить все данные БД:
```bash
docker-compose down -v
```

## 📝 Импорт данных с сервера (опционально)

Если нужно импортировать данные с сервера:

```bash
# 1. Экспорт с сервера
mysqldump -h 147.45.146.200 -u server -p lk_tk82_ru > dump.sql

# 2. Импорт в локальную БД
docker-compose exec -T mysql mysql -ularavel -plaravel_password lk_tk82_ru < dump.sql
```

## 🚀 Управление контейнерами

```bash
# Запустить
docker-compose up -d

# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Просмотр логов
docker-compose logs -f app
docker-compose logs -f mysql
```
