<div class="{{ $class ?? '' }}">
  <x-form.input 
    placeholder="{{ $placeholder ?? '' }}"
    name="{{ $name ?? '' }}"
    label="{{ $label ?? '' }}"
    labelClass="{{ $labelClass ?? '' }}"
  />
</div>